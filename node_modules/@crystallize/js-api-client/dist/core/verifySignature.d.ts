import { CrystallizeSignature } from '../types/signature';
export type SimplifiedRequest = {
    url?: string;
    method?: string;
    body?: any;
};
export type CreateSignatureVerifierParams = {
    sha256: (data: string) => string;
    jwtVerify: (token: string, secret: string, options?: any) => CrystallizeSignature;
    secret: string;
};
export declare const createSignatureVerifier: ({ sha256, jwtVerify, secret }: CreateSignatureVerifierParams) => (signature: string, request: SimplifiedRequest) => any;
