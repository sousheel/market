import { ClientInterface } from './client';
export declare enum NavigationType {
    Tree = 0,
    Topics = 1
}
export type TreeFetcher = (path: string, language: string, depth: number, extraQuery?: any, perLevel?: (currentLevel: number) => any) => Promise<any>;
export declare function buildNestedNavigationQuery(type: NavigationType, path: string, depth: number, extraQuery?: any, perLevel?: (currentLevel: number) => any): string;
export declare function createNavigationFetcher(client: ClientInterface): {
    byFolders: TreeFetcher;
    byTopics: TreeFetcher;
};
