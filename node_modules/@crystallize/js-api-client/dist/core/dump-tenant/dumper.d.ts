import { MassClientInterface } from '../massCallClient';
export type DumperOptions = {
    tenantIdentifier: string;
    topicsDepth?: number;
    treeDepth?: number;
};
export type TenantDescription = {
    tenant: {
        id: string;
        identifier: string;
        staticAuthToken: string;
    };
    shapes?: any[];
    priceVariants?: any[];
    stockLocations?: any[];
    subscriptionPlans?: any[];
    languages: {
        default: string;
        availableLanguages: any[];
    };
    vatTypes?: any[];
    topics: Record<string, any[]>;
    grids: Record<string, any[]>;
    items: Record<string, any[]>;
};
export declare const createDumper: (apiClient: MassClientInterface, options: DumperOptions) => {
    dump: () => Promise<any>;
};
